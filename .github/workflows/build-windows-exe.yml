name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 安装 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 安装依赖库
        working-directory: pst_tool
        run: |
          pip install -r requirements.txt

      - name: 用 PyInstaller 打包成单文件 exe
        working-directory: pst_tool
        run: |
          pyinstaller --onefile --windowed --name "PST邮件搜索工具" --add-data "assets;assets" --hidden-import win32com.client --hidden-import pythoncom --hidden-import pywintypes pst_search_tool.py

      - name: 上传打包结果
        uses: actions/upload-artifact@v4
        with:
          name: PST邮件搜索工具-windows-exe
          path: pst_tool/dist/*.exe
